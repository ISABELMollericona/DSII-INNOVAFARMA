/**
 * This file includes polyfills needed by Angular and is loaded before the app.
 * You can add your own extra polyfills to this file.
 *
 * Zone JS is required by default for Angular's change detection. Ensure it's
 * imported here so the app renders and the router works correctly.
 */

import 'zone.js'; // Included with Angular (recommended)

// If you need zone error stack frames during development uncomment the
// following line (can impact performance in production):
// import 'zone.js/plugins/zone-error';
