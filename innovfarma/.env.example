# Ejemplo de variables de entorno
DATABASE_URL=mysql://user:password@localhost/innovfarma_db
FLASK_ENV=development
SECRET_KEY=change-me

# Habilita (True) el prefill demo en el frontend auth modal cuando quieras forzar
# las credenciales demo incluso en hosts externos (ej. Render). Útil para QA.
FRONTEND_DEMO_PREFILL=False

# Opciones de cookies de sesión (opcional)
SESSION_COOKIE_DOMAIN=
SESSION_COOKIE_SAMESITE=Lax
SESSION_COOKIE_SECURE=False
